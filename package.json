{
  "name": "vehservoir",
  "version": "0.1.0",
  "description": "Vehicle reservation integration slack-ation",
  "scripts": {
    "server:dev": "npm run start:dev --prefix server",
    "client:dev": "DANGEROUSLY_DISABLE_HOST_CHECK=true npm run start --prefix frontend",
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\""
  },
  "devDependencies": {
    "concurrently": "^7.4.0"
  },
  "dependencies": {
    "@nestjs/config": "^2.2.0",
    "react-icons": "^4.4.0"
  }
}
